<section class="my-4 max-w-screen-xl mx-auto">
  <%= render 'shared/errors', { obj: @lot } %>

  <h2 class="mb-4 title dark:text-white text-2xl font-semibold tracking-wide">Editar Lote - <%= @lot.batch_code %></h2>

  <%= render 'form' %>
</section>
